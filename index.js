const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const inc = a => a + 1;
const dec = n => n - 1;
const makeInt = n => {
  
}

const preserveDecimal = n => {
  
}